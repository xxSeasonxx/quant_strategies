# Scoring Method

Selected-15 ranking method version: `research_handoff_rank_v1_selected_15`.

The preserved `selected_15` score is a blended handoff score. The new_15 rerun score is the single-window `locked_recent_2026` smoke score produced by the current `quant_strategies` runner. Both are loop feedback only, not market evidence.
