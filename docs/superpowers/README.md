# Superpowers Planning Archive

Files under this directory are historical planning/spec artifacts. They are not
the active source of truth for repository contracts, validation semantics, or
workflow commands.

Use current source code, `README.md`, `AGENTS.md`, and committed review docs for
active behavior. When a historical plan conflicts with current code or docs, the
historical plan is stale by definition.
